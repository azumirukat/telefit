<%- include('base', { title: 'Workout Routines', content: `
    <section class="main-section">
        <h2 class="text-2xl mb-6">Workout Routines</h2>
        <div>
            <label for="workout-selector" class="block text-gray-700 mb-2">Select Workout:</label>
            <select id="workout-selector" class="p-2 border border-gray-300 rounded" onchange="loadWorkout()">
                <option value="workout1">Workout 1</option>
                <option value="workout2">Workout 2</option>
                <!-- Add more options as needed -->
            </select>
        </div>
        <table id="workoutTable" class="mt-4">
            <thead>
                <tr>
                    <th>Exercise</th>
                    <th>Reps</th>
                    <th>Sets</th>
                </tr>
            </thead>
            <tbody>
                <!-- Workout rows will be added here dynamically -->
            </tbody>
        </table>
        <button class="bg-blue-500 text-white px-4 py-2 rounded mt-4" onclick="addExercise()">Add Exercise</button>
        <button class="bg-red-500 text-white px-4 py-2 rounded mt-4" onclick="deleteWorkout()">Delete Workout</button>
        <button class="bg-green-500 text-white px-4 py-2 rounded mt-4" onclick="saveWorkout()">Save Workout</button>
    </section>
    <script>
        function loadWorkout() {
            // Load workout data based on selected workout
        }

        function addExercise() {
            const table = document.getElementById('workoutTable').getElementsByTagName('tbody')[0];
            const row = table.insertRow();
            const exerciseCell = row.insertCell(0);
            const repsCell = row.insertCell(1);
            const setsCell = row.insertCell(2);

            exerciseCell.innerHTML = '<input type="text" class="p-2 border border-gray-300 rounded">';
            repsCell.innerHTML = '<input type="number" class="p-2 border border-gray-300 rounded">';
            setsCell.innerHTML = '<input type="number" class="p-2 border border-gray-300 rounded">';
        }

        function deleteWorkout() {
            // Delete the selected workout
        }

        function saveWorkout() {
            // Save the current workout
        }
    </script>
` }) %>
